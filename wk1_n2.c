#include <stdio.h>
#include <stdlib.h>

int main () {
  int first,second;
  printf ("Enter two integers: ");
  scanf("%d %d", &first, &second);
  printf("The user entered %d and %d", first, second);
return 0;
}
